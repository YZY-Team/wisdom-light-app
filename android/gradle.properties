# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

org.gradle.caching=true

org.gradle.configureondemand=true

org.gradle.profile=true

# Specifies the JVM arguments used for the daemon process.
# 指定用于Gradle守护进程的JVM参数，主要用于调整内存设置
# 2048m表示最大堆内存，512m表示最大元空间大小
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -XX:+UseParallelGC -XX:+HeapDumpOnOutOfMemoryError

# When configured, Gradle will run in incubating parallel mode.
# 启用并行构建模式，可以显著提高多模块项目的构建速度
# 利用多核CPU同时执行多个任务
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# 启用AndroidX库支持，这是Google推荐的Android支持库
android.useAndroidX=true

# Enable AAPT2 PNG crunching
# 启用PNG图片压缩，减小APK体积，但可能增加构建时间
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# 指定构建的CPU架构，影响APK大小和兼容性
# arm64-v8a: 大多数现代Android设备(2015年后的中高端手机)使用的64位ARM架构
# 仅构建一种架构可大幅减少构建时间和APK体积
reactNativeArchitectures=arm64-v8a

# Use this property to enable support to the new architecture.
# 启用React Native新架构(Fabric和TurboModules)
# 新架构提供更好的性能和更流畅的界面交互体验
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
# 启用Hermes JavaScript引擎
# Hermes能减小APK体积、加快应用启动速度和提高JavaScript执行性能
hermesEnabled=true

# Enable GIF support in React Native images (~200 B increase)
# 启用GIF图片支持，增加约200字节大小
expo.gif.enabled=true

# Enable webp support in React Native images (~85 KB increase)
# 启用WebP图片格式支持，比PNG更小但质量相近，增加约85KB
expo.webp.enabled=true

# Enable animated webp support (~3.4 MB increase)
# 禁用动画WebP支持，因为iOS不支持
# 启用会增加约3.4MB大小
expo.webp.animated=false

# Enable network inspector
# 启用网络请求检查器，方便调试网络请求
# 仅开发环境有效
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
# 禁用旧版打包方式来压缩APK中的原生库
# 使用新的打包方式可以得到更小的APK
expo.useLegacyPackaging=false

# 指定Android SDK编译版本(API 35, Android 15)
android.compileSdkVersion=35

# 指定目标SDK版本，决定应用运行行为
android.targetSdkVersion=35

# 指定构建工具版本
android.buildToolsVersion=35.0.0

# 额外的Maven仓库，当前为空
android.extraMavenRepos=[]